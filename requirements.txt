# =============================================================
#  v1fresh — Python dependencies
#
#  INSTALL EVERYTHING WITH THIS ONE COMMAND (copy & run):
#
#      pip install -r requirements.txt
#
#  Optional but recommended — make a virtual environment first:
#      Windows:    python -m venv venv && venv\Scripts\activate
#      Mac/Linux:  python3 -m venv venv && source venv/bin/activate
#
#  This app stores its data in MySQL (so you can view it in phpMyAdmin via
#  XAMPP). Before running it:
#    1. Start Apache + MySQL in the XAMPP control panel.
#    2. Open http://localhost/phpmyadmin and create a new, empty database
#       named exactly "v1fresh" (no tables needed — the app creates them).
#
#  Then run the site:
#      python seed.py     # first time only — creates tables + sample data
#      python app.py      # starts http://localhost:5000
# =============================================================

Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.3
gunicorn==22.0.0
Authlib==1.3.2
requests==2.32.3
python-dotenv==1.0.1
PyMySQL==1.1.1
